import React, { useState } from "react";

export default function PlayGame(props) {
  const { time, paused, isInfinite, gameState } = props;

  return (
    <div className="mx-auto">
      <h1>hello</h1>
    </div>
  );
}
